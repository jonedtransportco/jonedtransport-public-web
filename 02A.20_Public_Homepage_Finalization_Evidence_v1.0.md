# 02A.20 Public Homepage Finalization Evidence v1.0

Version: 1.0  
Lifecycle: Under_Review  
Authorization state: AUTHORIZED TO CLOSE PUBLIC HOMEPAGE FINALIZATION ONLY

## Objective

Document and regularize the next public homepage implementation state reached after `02A.19`, specifically:

- refinement of `jonedtransport.com` into a more final public corporate homepage;
- clearer public navigation for company, services, recruiting, and contact;
- stronger explicit separation between public website and internal portal;
- preservation of the mock-only and no-real-connection boundaries.

## Scope captured

This package records the completed frontend work for:

- `jonedtransport.com` public homepage refinement;
- public navigation refinement;
- public recruiting entry-point presentation refinement;
- supporting validation updates.

## Traceability inside `02A`

- preserves the domain split regularized in `02A.16`
- extends the public recruiting structure regularized in `02A.17`
- extends the driver public refinement regularized in `02A.19`
- remains inside the public-site-only scope without real backend activation

## Implemented state

### 1. Public homepage refinement

`app/page.tsx` now presents a more final public company homepage for `jonedtransport.com`.

The homepage now more clearly presents:

- who JONED is as a public-facing transportation company;
- public services and engagement paths;
- recruiting entry points for `Owner Operators` and `Drivers`;
- contact and outreach framing;
- explicit differentiation between public website and internal portal.

### 2. Public navigation refinement

The homepage navigation now better reflects the expected public structure:

- `About`
- `Services`
- `Recruiting`
- `Company`
- `Contact`

This better separates public discovery from portal-only operational access.

### 3. Public-versus-portal separation reinforcement

The homepage now reinforces that:

- `jonedtransport.com` is the public website;
- `portal.jonedtransport.com` is the controlled internal portal;
- recruiting begins from the public website, not from the portal shell.

### 4. Validation refresh

`tests/rendered-html.test.mjs` now asserts the refined homepage framing and public homepage markers.

## Files covered by this package

- `app/page.tsx`
- `tests/rendered-html.test.mjs`
- `02A.20_Public_Homepage_Finalization_Evidence_v1.0.md`
- `02A.20_Evidence_v1.0.json`
- `02A.20_SHA-256_Manifest_v1.0.txt`

## Validation performed

- build result: passed
- rendered-html test suite result: passed
- executed suite result: `7/7` tests passing
- route surface confirmed at build output:
  - `/`
  - `/drivers`
  - `/owner-operators`
  - `/portal`
  - `/api/v1/reports`

## Explicit non-operational confirmation

The implemented homepage remains non-operational with respect to real backend workflows:

- no applicant data persistence;
- no document upload storage;
- no real submission endpoint;
- no portal workflow activation;
- no screening or approval automation;
- no production backend connection.

## Explicit zero-real-connection confirmation

Confirmed for this package:

- zero Azure SQL real connections;
- zero Microsoft Graph operational connections;
- zero SharePoint operational runtime connections;
- zero email runtime activation;
- zero intake-document runtime activation;
- zero Gusto, PNC, OneRail, Frayt, or ELD connections;
- zero business automation activation.

## Non-authorized actions not executed

- no real intake activation;
- no public applicant-record creation;
- no operational onboarding activation;
- no production opening of additional portal domains;
- no real review workflow activation inside the portal;
- no backend API switch.

## Stop point

This package closes the current public homepage finalization only. It does not authorize real intake runtime, real uploads, portal review workflow, or production integration.

Return control to `00.2 - JONED Strategy & Architecture Office | Continuidad Operativa II`.
