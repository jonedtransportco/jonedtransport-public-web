# 02A.16 Public Web Regularization Evidence v1.0

Version: 1.0  
Lifecycle: Under_Review  
Authorization state: AUTHORIZED TO REGULARIZE COMPLETED PUBLIC-WEB AND DOMAIN-SPLIT WORK ONLY

## Objective

Regularize the public-web and domain-separation work already completed in the active implementation chat so the current state is traceable under the `02A` workstream and no longer exists only as in-chat execution.

## Scope regularized

This regularization package records the implementation state reached for the following areas:

- mandatory separation between:
  - `jonedtransport.com` as the public corporate website;
  - `portal.jonedtransport.com` as the internal or controlled portal;
- public homepage refinement for the company-facing web surface;
- public `Owner Operators` page refinement aligned to the approved intake model direction;
- supporting DNS import artifact for GoDaddy review configuration;
- validation updates proving the public and owner-operator pages still render under the approved frontend baseline.

## Traceability inside `02A`

- aligns with `02A.15_Contractor_Intake_Requirements_Normalization_v1.0.md`
- aligns with `02A.15A_Contractor_Intake_Route_Matrix_v1.0.md`
- aligns with `02A.15B_Contractor_Intake_Form_and_Upload_Blueprint_v1.0.md`
- implements the public-page direction prepared in `02A.15C_Owner_Operators_Public_Page_Blueprint_v1.0.md`
- preserves the frontend boundary and no-real-backend constraints already established across `02A.06`, `02A.07`, `02A.08`, `02A.11`, `02A.13`, and `02A.14`

## Implemented state captured by this package

### 1. Domain split

- `app/page.tsx` resolves the host and routes:
  - `portal.jonedtransport.com` to the portal experience;
  - all other approved public hosts to the public corporate website;
- `app/owner-operators/page.tsx` preserves the public-only intake path and redirects away from portal-host usage;
- `README.md` and `ARCHITECTURE.md` explicitly state that the public website and portal must remain separate final domain targets.

### 2. Public homepage

The homepage now presents a more definitive company-facing structure:

- services;
- company structure;
- operating model;
- recruiting entry points;
- public-to-portal separation;
- public contact direction.

The page no longer behaves as an ambiguous hybrid between marketing surface and internal portal shell.

### 3. `Owner Operators` public page

The public page now includes:

- clearer fit and route framing;
- expanded profile definitions;
- `Why work with JONED` value section;
- preparation guidance with route-based cautioning;
- intake process explanation with no over-promise of operational activation;
- stronger start-application conversion sections at mid-page and footer.

### 4. DNS support artifact

`jonedtransport.com-root-and-portal-godaddy-import.txt` is preserved as the non-sensitive review artifact used to support:

- public root-domain pointing for `jonedtransport.com`;
- portal subdomain continuity for `portal.jonedtransport.com`;
- coexistence with Microsoft 365 DNS records;
- required TXT validation records for the Sites-hosted surface.

## Files covered by this regularization

- `app/page.tsx`
- `app/owner-operators/page.tsx`
- `app/globals.css`
- `tests/rendered-html.test.mjs`
- `README.md`
- `ARCHITECTURE.md`
- `jonedtransport.com-root-and-portal-godaddy-import.txt`
- `02A.15C_Owner_Operators_Public_Page_Blueprint_v1.0.md`

## Validation performed

- build result: passed
- rendered-html test suite result: passed
- executed suite result: `6/6` tests passing
- public homepage render assertion updated to the finalized company-facing copy
- `Owner Operators` source assertions updated to the finalized public-page structure

## Explicit zero-real-connection confirmation

Confirmed for this regularization package:

- zero Azure SQL real connections;
- zero Microsoft Graph operational connections;
- zero SharePoint operational runtime connections;
- zero email runtime connections;
- zero document-intake runtime connections;
- zero payments, Gusto, PNC, OneRail, Frayt, or ELD connections;
- zero business automation activation;
- zero production opening of additional portal domains.

## Gaps intentionally not closed here

- no public `Drivers` page was implemented in this package;
- no dynamic intake form was activated from the public `Owner Operators` page;
- no internal applicant-review workflow was activated from the portal;
- no real authentication, membership, or operational authorization model was changed;
- no production backend integration was enabled.

## Reason for this regularization package

Work in the active implementation chat advanced materially before every step had been wrapped into a formally published package. This document regularizes the current state so the completed work is auditable, non-sensitive, and attached back to the `02A` evidence chain.

## Stop point

This package records and regularizes already-completed public-web work only. It does not authorize new production opening, new backend activation, or any operational integration.

Return control to `00.2 - JONED Strategy & Architecture Office | Continuidad Operativa II`.
